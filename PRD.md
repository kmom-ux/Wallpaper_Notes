# Wallpaper_Notes — 产品需求文档

> 版本：v0.1 (MVP)  
> 创建日期：2026-05-15  
> 状态：已审查

---

## 1. 产品定位

桌面小组件，把便签内容直接贴在桌面上。不需要打开任何软件就能看到，退到桌面就能写。

**核心价值：**
- **提醒** — 每日安排贴在桌面，一眼可见
- **记录** — 低摩擦捕捉灵感，不需要经历「打开软件→新建文件→命名」的步骤
- **美学** — 浪漫文字、诗句直接显示在桌面，兼顾功能与美感

---

## 2. 架构设计

### 2.1 整体映射关系

```
一个标签页 ═══ 一个 .md 文件
```

文件系统是唯一的真相源（source of truth）。

### 2.2 操作与映射解耦

```
┌─────────────────────┐       ┌──────────────────────┐
│  GUI 操作层          │       │  文件系统映射层        │
│                      │       │                        │
│  • 新建标签→创建.md  │       │  • 启动时扫描 .md 文件  │
│  • 删除标签→删除.md  │ ←─→  │  • 实时监听文件变化     │
│  • 重命名→重命名.md  │       │  • 新增/删除/改自动同步  │
│  • 编辑内容→写回.md  │       │  • 与 GUI 操作各自独立  │
└─────────────────────┘       └──────────────────────┘
```

GUI 只管操作，映射层只管检测，两者通过文件系统自然同步。

### 2.3 项目文件结构

```
Wallpaper_Notes/
├── notes/                  ← 便签数据目录（核心）
│   ├── 每日待办.md
│   ├── 今日诗句.md
│   ├── 灵感札记.md
│   └── ...（用户自由增删）
│
├── config.json             ← 功能配置（窗口、快捷键等）
├── theme.json              ← 样式配置（颜色、字体、透明度等）
│
├── main.py                 ← 主程序入口
├── requirements.txt
├── README.md
└── PRD.md                  ← 本文件
```

---

## 3. 功能规格（MVP）

### 3.1 窗口系统

| 项目 | 规格 |
|------|------|
| 窗口形态 | 单窗口，顶部标签栏切换不同便签 |
| 窗口层级 | 默认吸附在桌面层，不遮挡其他应用窗口 |
| 窗口大小 | 用户可拖拽边缘自由调节 |
| 窗口位置 | 首次启动居中；支持拖拽移动，位置记忆到 config.json |
| 内容溢出 | 内容自适应窗口宽度，高度超出时显示滚动条 |
| 窗口装饰 | 无标题栏、无最小化/最大化按钮，保持极简 |
| 窗口显隐 | 不提供「关闭」按钮；所有窗口操作通过系统托盘 |

### 3.2 标签系统（便签管理）

| 项目 | 规格 |
|------|------|
| 标签识别 | 启动时扫描 `notes/` 下所有 `.md` 文件，每个文件对应一个标签 |
| 新建标签 | 标签栏右侧 `+` 按钮 → 弹出命名框 → 创建 `.md` 文件 → 自动切换 |
| 删除标签 | 标签右键菜单 → 确认 → 删除对应 `.md` 文件 |
| 重命名标签 | 标签右键菜单 → 原地改名 → 同步重命名 `.md` 文件 |
| 标签切换 | 点击标签栏切换；支持快捷键 `Ctrl+Tab` / `Ctrl+Shift+Tab` |
| 文件检测 | 运行中持续监听 `notes/` 文件夹变化（新增/删除/修改自动同步） |

### 3.3 内容编辑

| 项目 | 规格 |
|------|------|
| 显示模式 | 渲染 Markdown，以优雅样式展示内容 |
| 编辑模式 | **原地切换** — 双击便签内容区，同一位置变为编辑框 |
| 编辑内容 | 编辑模式显示原始 Markdown 文本 |
| 保存退出 | 按 `Esc` → 自动保存 `.md` 文件 → 切回显示模式 |
| 自动保存 | 编辑过程中可定期/防抖自动保存（防止意外丢失） |

### 3.4 快捷键系统

| 快捷键 | 行为 |
|--------|------|
| 全局快捷键（可配置） | 窗口置顶 + 编辑框聚焦 + 自动进入编辑模式 |
| `Esc` | 保存并退出编辑，窗口回到桌面层 |
| `Ctrl+Tab` | 切换到下一个标签 |
| `Ctrl+Shift+Tab` | 切换到上一个标签 |

**实现方式：** 使用 Windows API (`RegisterHotKey`) 注册系统级全局热键，零额外依赖。

### 3.5 系统托盘

| 项目 | 规格 |
|------|------|
| 托盘图标 | 启动时自动创建系统托盘图标 |
| 右键菜单 | 包含「设置」「退出」等操作 |
| 窗口显隐 | 托盘菜单控制窗口的显示/隐藏，桌面不设任何按钮 |
| 退出确认 | 退出时确认，防止误操作导致丢失编辑中内容 |

### 3.6 开机自启

| 项目 | 规格 |
|------|------|
| 实现方式 | 写入 Windows 注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` |
| 开关位置 | 设置界面或托盘菜单可选启用/禁用 |

### 3.7 AI 集成

| 项目 | 规格 |
|------|------|
| 写入方式 | AI Agent 直接写入 `notes/` 下的 `.md` 文件 |
| 刷新机制 | 文件系统监听自动检测变化并刷新显示 |
| 使用场景 | 晨间仪式：Agent 定时写入诗句/文学句子 |
| 数据安全 | AI 写入和用户编辑同时发生时，后保存者覆盖前保存者（MVP阶段） |

---

## 4. 配置文件规范

### 4.1 config.json（功能配置）

```jsonc
{
  "window": {
    "x": null,          // 窗口X坐标，null表示居中
    "y": null,          // 窗口Y坐标，null表示居中
    "width": 400,       // 窗口宽度
    "height": 500       // 窗口高度
  },
  "hotkey": {
    "modifiers": ["ctrl", "shift"],
    "key": "n",
    "description": "快捷键：唤起便签置顶编辑"
  },
  "behavior": {
    "autostart": true,               // 开机自启
    "auto_save_interval_ms": 3000     // 编辑时自动保存间隔
  }
}
```

### 4.2 theme.json（样式配置）

```jsonc
{
  "window": {
    "background_color": "rgba(255, 255, 255, 0.85)",
    "border_radius": 12,              // 窗口圆角
    "border_color": "rgba(200, 200, 200, 0.5)",
    "border_width": 1,
    "shadow": {
      "enabled": true,
      "color": "rgba(0, 0, 0, 0.15)",
      "blur": 20,
      "offset_x": 0,
      "offset_y": 4
    }
  },
  "tab_bar": {
    "background_color": "rgba(245, 245, 245, 0.9)",
    "text_color": "#666666",
    "active_text_color": "#333333",
    "active_indicator_color": "#4A90D9",
    "font_size": 14,
    "font_family": "Microsoft YaHei",
    "height": 36,
    "padding": "8px 12px"
  },
  "content": {
    "background_color": "transparent",
    "text_color": "#333333",
    "font_size": 16,
    "font_family": "Microsoft YaHei",
    "line_height": 1.6,
    "padding": "16px"
  },
  "editor": {
    "background_color": "#FAFAFA",
    "text_color": "#333333",
    "font_size": 15,
    "font_family": "Cascadia Code, Consolas, monospace",
    "caret_color": "#4A90D9"
  },
  "scrollbar": {
    "width": 6,
    "handle_color": "rgba(0, 0, 0, 0.2)",
    "handle_hover_color": "rgba(0, 0, 0, 0.4)",
    "track_color": "transparent"
  }
}
```

配置文件采用 `config.json` + `theme.json` 分离策略：
- **config.json**：功能行为相关，用户一般不动
- **theme.json**：视觉样式相关，用户可以自由修改换皮肤

---

## 5. 用户流程

### 5.1 启动流程

```
启动程序
  ↓
创建系统托盘图标
  ↓
读取 config.json / theme.json
  ↓
扫描 notes/ 加载 .md 文件生成标签
  ↓
窗口居中显示桌面层（渲染第一个标签内容）
```

### 5.2 日常查看

```
桌面常态 → 窗口显示在桌面层
         → 主动切换到其他标签查看不同便签内容
         → 内容随 AI 写入实时自动刷新
```

### 5.3 快速记录

```
工作中有灵感
  ↓
按快捷键（如 Ctrl+Shift+N）
  ↓
窗口置顶 + 自动进入编辑模式 + 聚焦到编辑框
  ↓
直接打字编辑（MD格式）
  ↓
按 Esc
  ↓
自动保存到对应的 .md 文件
  ↓
窗口回到桌面层，显示渲染后的内容
```

### 5.4 AI 晨间仪式

```
定时任务触发
  ↓
Agent 写入 notes/今日诗句.md
  ↓
应用检测到文件变化
  ↓
自动刷新标签内容显示
  ↓
用户打开电脑 → 桌面上已经显示新的诗句
```

### 5.5 管理便签

```
右键标签 → 新建 / 重命名 / 删除
右键托盘 → 设置 / 退出
```

---

## 6. 非 MVP（后续迭代）

- 每个标签独立的样式配置（不同颜色/字体/透明度的便签）
- 图形化设置界面
- 标签切换动画
- 编辑器语法高亮
- AI与用户编辑的冲突合并策略
- 多平台支持（Linux/macOS）

---

## 7. 技术栈建议

| 组件 | 建议 |
|------|------|
| UI 框架 | PySide6（Qt for Python） |
| Markdown 渲染 | QTextBrowser 或 mistune+QTextDocument |
| 文件监听 | watchdog（PollingObserver 兼容 Windows） |
| 全局快捷键 | Windows API（ctypes + RegisterHotKey） |
| 打包分发 | PyInstaller（--onefile --noconsole） |
| 开机自启 | Windows 注册表写入 |

---

## 8. 开源计划

- 托管平台：GitHub
- 许可证：MIT（推荐）
- README 主要内容：
  - 产品介绍与截图
  - 安装方式（直接下载 exe / 源码运行）
  - 配置文件说明（重点：theme.json 自定义皮肤）
  - AI 集成指南（如何让 Agent 写入便签）
  - 开发指南

---

*本 PRD 为 MVP 版本，后续根据使用反馈迭代。*
