# Wallpaper_Notes — 架构设计

> 版本：v0.1  
> 创建日期：2026-05-15  
> 基于：PRD.md

---

## 1. 项目文件结构

```
Wallpaper_Notes/
│
├── main.py              ← 入口：启动应用
├── app.py               ← 应用主控：初始化各模块，连接信号与槽
│
├── window.py            ← 主窗口 UI 布局（UI层）
│                         QTabWidget（标签栏）+ QStackedWidget（内容区）
│
├── ui_components.py     ← UI 组件（UI层）
│                         样式应用、主题加载、未来手搓自定义widget的改造点
│
├── notes_manager.py     ← 文件映射层（后端）
│                         扫描 notes/、监听文件变化、新增/删除/重命名同步
│
├── config.py            ← 配置加载/保存（后端）
│                         处理 config.json + theme.json
│
├── renderer.py          ← Markdown 渲染（后端）
│                         Markdown → HTML → 显示
│
├── tray.py              ← 系统托盘（后端）
│                         托盘图标、右键菜单（设置/退出）
│
├── hotkey.py            ← 全局快捷键（后端）
│                         Windows API 注册系统级热键
│
├── notes/               ← 便签数据目录
│   ├── 每日待办.md
│   ├── 今日诗句.md
│   └── 灵感札记.md
│
├── config.json          ← 功能配置
├── theme.json           ← 样式配置
├── requirements.txt     ← 依赖清单
├── .gitignore
├── README.md
└── PRD.md
```

---

## 2. 模块职责

### 2.1 UI 层

| 模块 | 职责 | 说明 |
|------|------|------|
| `window.py` | 主窗口框架 | 整体布局：顶部标签栏 + 中间内容区，窗口行为（桌面层/置顶/拖拽/调整大小） |
| `ui_components.py` | UI 组件与样式 | 加载 theme.json 生成 QSS 样式，应用主题到所有控件。未来如果 QTabWidget 不满足需求，手搓自定义 TabBar 的改造点就在这里 |

**UI 与后端分离原则**：所有 UI 相关组件集中在 window.py + ui_components.py，后端逻辑不直接操作 UI 元素。

### 2.2 后端层

| 模块 | 职责 |
|------|------|
| `app.py` | 应用主控，初始化所有模块，建立信号-槽连接 |
| `notes_manager.py` | 启动时扫描 `notes/` 下所有 `.md` 文件生成标签列表；运行时监听文件变化（新增/删除/修改），同步更新 |
| `config.py` | 读取/写入 config.json（功能配置）和 theme.json（样式配置） |
| `renderer.py` | 将 Markdown 文本转换为可显示的 HTML，供 QTextBrowser 渲染 |
| `tray.py` | 创建系统托盘图标，管理右键菜单（设置/退出） |
| `hotkey.py` | 使用 Windows API (`RegisterHotKey`) 注册全局快捷键，零额外依赖 |

---

### 2.3 模块 Idea 动因（工程定义）

以下对每个模块的**本质职责、核心接口、依赖关系、生命周期边界**做具体定义，作为编码阶段的直接约束。

---

#### main.py — 进程入口

**本质职责：** 创建 QApplication 实例，实例化 App 主控对象并调用 run()。

```python
# 核心结构
if __name__ == '__main__':
    app = QApplication(sys.argv)
    controller = App()
    controller.run()
    sys.exit(app.exec())
```

**边界：** 不包含任何业务逻辑，不做初始化以外的操作。

---

#### app.py — 应用生命周期控制器

**本质职责：**
1. 按固定顺序初始化各模块
2. 跨模块的信号-槽总装（wiring）
3. 启动/停止文件监听
4. 响应退出事件，执行逆序清理

**核心接口：**
```python
class App:
    def __init__(self)              # 仅创建依赖对象，不启动
    def run(self) -> None            # 全流程：config加载 → notes扫描 → window创建 → tray/hotkey注册 → 文件监听启动
    def shutdown(self) -> None       # 逆序清理：停止监听 → 注销热键 → 保存配置 → 退出事件循环
```

**信号-槽总装责任（由 run() 方法执行）：**
- `notes_manager.note_changed(filepath)` → `window.refresh_current_tab(filepath)`
- `notes_manager.note_added(filepath)` → `window.add_tab(filepath)`
- `notes_manager.note_deleted(filepath)` → `window.remove_tab(filepath)`
- `hotkey_manager.activated` → `window.bring_to_front_and_edit()`
- `tray.exit_requested` → `self.shutdown()`

**边界：** 不直接操作 UI 组件，不直接读写文件。仅做初始化编排和信号连接。

---

#### window.py — 桌面窗口管理器

**本质职责：**
1. 持有 QTabWidget（标签栏）+ QStackedWidget（内容区），管理两者的联动
2. 实现桌面层与置顶层的切换
3. 实现无边框窗口的拖拽移动与边缘缩放
4. 完成显示模式与编辑模式的切换

**核心接口：**
```python
class WallpaperWindow(QWidget):
    def __init__(self, theme: dict, notes: list[NoteInfo])
    def bring_to_front_and_edit(self) -> None   # 置顶 + 聚焦编辑框（被 hotkey 触发）
    def return_to_desktop_layer(self) -> None    # 回到桌面层（编辑完毕调用）
    def refresh_current_tab(self, filepath: str) -> None  # 重新渲染当前标签
    def add_tab(self, filepath: str) -> None     # 新增标签页
    def remove_tab(self, filepath: str) -> None   # 移除标签页
    def set_theme(self, theme: dict) -> None      # 应用新主题
```

**内部信号（自处理，不外发）：**
- 双击内容区 → 切入编辑模式
- Esc 键（编辑模式中）→ 保存 → 切回显示模式
- 标签关闭请求 → 触发文件删除
- 标签重命名 → 触发文件重命名

**依赖：** config（theme）、notes_manager（文件操作）、renderer（MD→HTML）

**边界：** 不直接读写文件系统。不做文件格式转换。

---

#### ui_components.py — UI 组件工厂与主题引擎

**本质职责：**
1. 根据 theme.json 生成 QSS 样式表字符串
2. 提供预配置的 UI 组件实例（已应用主题的 QTabWidget、QTextBrowser 等）
3. 作为「自定义组件的隔离层」——未来超出 QTabWidget 能力的改造点

**核心接口：**
```python
def generate_window_qss(theme: dict) -> str        # 窗口级样式
def generate_tab_bar_qss(theme: dict) -> str        # 标签栏样式
def generate_content_qss(theme: dict) -> str         # 内容区样式
def generate_editor_qss(theme: dict) -> str          # 编辑器样式
def generate_scrollbar_qss(theme: dict) -> str       # 滚动条样式

def create_tab_bar(parent, theme: dict) -> QTabWidget  # 已应用样式的标签栏
```

**依赖：** config（theme 数据）

**MVP 行为：** 仅生成 QSS 字符串并应用到标准 QWidgets。未来标准控件不够用时，隔离层保障改造不影响 window.py 的布局逻辑。

---

#### notes_manager.py — 文件系统到便签列表的映射器

**本质职责：**
1. 维护 `notes/` 目录的实时镜像：`list[NoteInfo]` 与目录下的 `.md` 文件保持最终一致
2. 提供 CRUD 方法——直接操作文件系统
3. 启动 watchdog 监听目录变化，变化发生时发射信号
4. 对上游屏蔽文件路径、监听器等实现细节

**核心接口：**
```python
@dataclass
class NoteInfo:
    filepath: str          # 完整路径
    filename: str          # 显示名（不含 .md 后缀）
    content: str           # 文件内容
    last_modified: float   # 时间戳

class NotesManager(QObject):
    # 信号
    note_changed = Signal(str)       # filepath
    note_added = Signal(str)         # filepath
    note_deleted = Signal(str)       # filepath

    def __init__(self, notes_dir: str)
    def scan_all(self) -> list[NoteInfo]           # 启动时全量扫描
    def create(self, filename: str) -> NoteInfo     # 创建 .md 文件
    def delete(self, filepath: str) -> None         # 删除 .md 文件
    def rename(self, old_path: str, new_name: str) -> str  # 重命名，返回新路径
    def save_content(self, filepath: str, content: str) -> None
    def read_content(self, filepath: str) -> str
    def start_watching(self) -> None                # 启动 watchdog 监听
    def stop_watching(self) -> None                 # 停止监听
```

**文件变化处理（内部逻辑）：**
```
watchdog 事件 → 过滤（仅 .md）→ 判断事件类型：
  → modified: 发射 note_changed
  → created:   发射 note_added
  → deleted:   发射 note_deleted
```

**边界：**
- 不关心文件内容格式（仅读写，不做解析）
- 不关心上游如何使用 NoteInfo
- 只操作 notes/ 目录

---

#### config.py — 配置持久化层

**本质职责：**
1. 读取 config.json + theme.json，合并为统一的配置对象
2. 提供类型安全的配置访问方法
3. 支持运行时写入（窗口位置记忆、开机自启状态更改等）

**核心接口：**
```python
class Config:
    def __init__(self, config_path: str, theme_path: str)
    def load(self) -> tuple[dict, dict]      # 返回 (config_dict, theme_dict)
    def save_config(self, config: dict)       # 持久化 config.json
    def save_theme(self, theme: dict)         # 持久化 theme.json
    def get_window_geometry(self) -> tuple    # 返回 (x, y, w, h)
    def set_window_geometry(self, x, y, w, h)
    def get_hotkey(self) -> tuple             # 返回 (modifiers, key)
    def is_autostart_enabled(self) -> bool
    def set_autostart(self, enabled: bool)    # 同时更新注册表
```

**边界：**
- 不感知 UI 存在
- 不做 JSON 字段校验（假设格式正确）
- autostart 注册表写入在此处理（MVP 简化）

---

#### renderer.py — Markdown 渲染管道

**本质职责：**
1. 接收 Markdown 文本 → 输出 HTML 字符串（供 QTextBrowser 显示）
2. 结合 theme.json 中的字体/字号/颜色信息生成内联 CSS
3. 作为渲染管道的隔离层——未来替换渲染库只需改此文件

**核心接口：**
```python
def render(md_text: str, theme: dict) -> str
    # 返回完整 HTML 片段（含内联样式）
```

**MVP 行为：** 直接调用 QTextBrowser.setMarkdown()，此接口备用。当需要扩展 MD 语法时，切换为 mistune/markdown-it-py 后此文件是唯一的改动点。

**边界：** 不做文件 IO，不感知 UI 层。

---

#### tray.py — 系统托盘控制器

**本质职责：**
1. 创建 QSystemTrayIcon 并显示在系统托盘区
2. 管理右键菜单（显示/隐藏窗口、退出）
3. 发射信号供 app.py 绑定

**核心接口：**
```python
class TrayManager(QObject):
    show_requested = Signal()
    exit_requested = Signal()
    settings_requested = Signal()

    def __init__(self, parent_widget: QWidget, icon_path: str)
    def show(self) -> None
    def hide(self) -> None
    def show_message(self, title: str, msg: str) -> None  # 气泡通知
```

**边界：**
- 仅发射信号，不控制窗口显示逻辑
- 不持有应用状态

---

#### hotkey.py — 全局热键注册器

**本质职责：**
1. 使用 Windows API（`RegisterHotKey` + `WM_HOTKEY`）注册系统级热键
2. 热键触发时发射 activated 信号
3. 零额外依赖，仅依赖 ctypes

**核心接口：**
```python
class HotkeyManager(QObject):
    activated = Signal()                     # 快捷键被按下

    def __init__(self, modifiers: list[str], key: str)
    def register(self) -> bool               # 注册热键
    def unregister(self) -> None             # 注销热键
    def rebind(self, modifiers: list[str], key: str) -> bool
```

**实现机制：** 通过 QAbstractNativeEventFilter 捕获 WM_HOTKEY 消息，发射 activated 信号。

**边界：** 只做「检测到快捷键 → 发信号」这一件事。

---

## 3. UI 架构

### 3.1 布局结构

```
主窗口 (QMainWindow / QWidget)
│
├── 标签栏: QTabWidget
│   ├── 每个标签页 = 一个 .md 文件
│   ├── 右侧 ⊕ 按钮用于新建标签
│   ├── 右键菜单（重命名/删除）
│   └── 样式由 theme.json 控制
│
└── 内容区: QStackedWidget
    ├── Index 0 — QTextBrowser（显示模式）
    │   渲染后的 Markdown 效果，美观展示
    │
    └── Index 1 — QPlainTextEdit（编辑模式）
        原始 Markdown 文本，可编辑
```

### 3.2 模式切换

```
显示模式 (QTextBrowser)  ←──────────→  编辑模式 (QPlainTextEdit)
     ↑                                        ↑
  双击内容区                              Esc 或失焦
     │                                        │
     └──── QStackedWidget.setCurrentIndex() ──┘
```

### 3.3 窗口行为

| 行为 | 实现方式 |
|------|----------|
| 无标题栏 | `setWindowFlags(Qt.FramelessWindowHint \| Qt.Tool \| Qt.WindowStaysOnBottomHint)` |
| 桌面层 | Qt.Tool 标志 + 窗口句柄操作 |
| 拖拽移动 | mousePressEvent 记录偏移 + mouseMoveEvent 更新位置 |
| 调整大小 | 边缘检测 + 鼠标光标变化 + resize 操作 |
| 置顶（快捷键） | 修改窗口标志为置顶 + show/raise/activate + 编辑框聚焦 |
| 返回桌面层（Esc） | 保存内容 → 修改窗口标志回桌面层 |

---

## 4. 核心交互流程

### 4.1 启动流程

```
main.py
  ↓
app.py 初始化
  ├── config.py 加载 config.json + theme.json
  ├── notes_manager.py 扫描 notes/ 生成标签列表
  ├── window.py 创建主窗口（QTabWidget + QStackedWidget）
  │   ├── 根据标签列表创建标签页
  │   └── 应用 theme.json 样式
  ├── tray.py 创建系统托盘
  ├── hotkey.py 注册全局快捷键
  └── 启动文件监听
```

### 4.2 编辑流程

```
快捷键 或 双击内容区
  ↓
hotkey.py 触发 / window.py 检测双击
  ↓
① 窗口置顶（叠加层）
② QStackedWidget 切换到 Index 1（编辑模式）
③ QPlainTextEdit 获取焦点
  ↓
用户编辑（MD 格式）
  ↓
用户按 Esc
  ↓
① 读取 QPlainTextEdit 内容
② notes_manager.py 写回对应的 .md 文件
③ QStackedWidget 切换回 Index 0（显示模式）
④ renderer.py 重新渲染 Markdown
⑤ QTextBrowser 显示
⑥ 窗口回到桌面层
```

### 4.3 文件变更检测

```
外部程序（AI Agent）写入 notes/xxx.md
  ↓
notes_manager.py 监听（watchdog）
  ↓
检测到文件变化
  ↓
① 如果是已有标签：重新读取内容 → 刷新 QTextBrowser
② 如果是新文件：创建新标签页 → 添加到 QTabWidget
③ 如果文件被删除：移除对应标签页
```

### 4.4 标签管理

```
新建标签：
  ⊕ 按钮 → 弹出命名框 → notes_manager 创建 .md → 刷新标签列表

删除标签：
  右键菜单 → 确认 → notes_manager 删除 .md → 刷新标签列表

重命名标签：
  右键菜单 → 原地改名 → notes_manager 重命名 .md → 刷新标签列表
```

---

## 5. 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| UI 框架 | PySide6 6.11.1 | Qt for Python，成熟稳定 |
| 标签栏 | QTabWidget（内置于 PySide6） | 开箱即用，加快开发；后续如需自定义，改造点在 ui_components.py |
| 内容容器 | QStackedWidget | 显示/编辑模式切换的标准做法 |
| 显示模式 | QTextBrowser | 自带 `.setMarkdown()`，零额外依赖 |
| 编辑模式 | QPlainTextEdit | 轻量文本编辑，支持等宽字体 |
| MD 渲染 | QTextBrowser.setMarkdown() | MVP 阶段够用；如需扩展，随时在 renderer.py 中替换为 mistune/markdown-it-py |
| 文件监听 | watchdog.PollingObserver | 跨平台，兼容 Windows 非原生文件系统 |
| 全局快捷键 | Windows API (ctypes + RegisterHotKey) | 零额外依赖 |
| 配置格式 | JSON | 人类可读写，AI 易解析 |
| 开机自启 | Windows 注册表 | 标准做法 |
| 打包分发 | PyInstaller | 轻量，--onefile --noconsole |

---

## 6. 关键设计决策

### 6.1 操作与映射解耦

```
GUI 操作层（新建/删除/重命名/编辑）
          ↕
      文件系统（notes/ 目录）
          ↕
文件映射层（扫描/监听/同步）
```

- GUI 只管执行操作（创建/删除/重命名文件）
- 映射层只管检测变化（扫描文件夹、监听 watchdog 事件）
- 两者通过文件系统自然同步，不直接耦合

### 6.2 配置分离

| 文件 | 用途 | 用户是否常改 |
|------|------|------------|
| `config.json` | 功能配置（窗口位置/快捷键/开机自启） | 一般不常改 |
| `theme.json` | 样式配置（颜色/字体/透明度/圆角） | 可自由修改换皮肤 |

### 6.3 UI 组件独立

所有 UI 相关代码集中在 `window.py` + `ui_components.py`，与后端业务逻辑解耦。未来如果 QTabWidget 不满足需求，只需修改 `ui_components.py` 中的组件实现，后端完全不受影响。

---

## 7. 数据流图

```
                    ┌─────────────┐
                    │  外部程序/AI │
                    │  (.md 写入) │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ notes/ 目录 │
                    │ (.md files) │
                    └──┬───┬───┬──┘
                       │   │   │
          ┌────────────┘   │   └────────────┐
          ▼                ▼                ▼
   notes_manager.py   用户编辑(Esc)    GUI操作(新建/删除)
          │                │                │
          ▼                ▼                ▼
   window.py 更新     写回 .md        创建/删除/重命名 .md
   QTabWidget         文件             文件
   QStackedWidget
   QTextBrowser
```
